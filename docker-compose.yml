version: '3.6'
services:
  server:
    build:
      context: ./server
    volumes:
     - ./server/src:/var/www/server/src
    environment:
      NODE_ENV: development
  client:
    build:
      context: ./client
    volumes:
     - ./client/src:/var/www/client/src
    environment:
      NODE_ENV: development
  nginx:
    build:
      context: ./tools/nginx
    ports:
     - "3003:80"
    depends_on:
     - server
     - client
